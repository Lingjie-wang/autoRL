interface:
  display_name: "RL Environment Integrator"
  short_description: "Reuse or integrate an RL environment behind the adapter contract"
  default_prompt: "Use $rl-env-integrator to reuse a compatible verified environment integration when possible, otherwise produce adapter, config, extracted spec, and smoke artifacts."
